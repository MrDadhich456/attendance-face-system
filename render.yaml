services:
  - type: web
    name: attendance-face-system
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/status
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_SSL
        value: "true"
      - key: FACE_MATCH_THRESHOLD
        value: "0.6"
      - key: ADMIN_PASSWORD
        sync: false
      - key: DATABASE_URL
        sync: false
